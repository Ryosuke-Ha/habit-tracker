# Database
DATABASE_URL=              # Supabase Transaction pooler URI (production) or sqlite:///./habit_tracker.db (development)

# CORS
FRONTEND_URL=              # フロントエンドのURL（例: http://localhost:3000）
